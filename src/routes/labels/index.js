import { Router } from "express";

const router = Router();

router.post("/create", () => {});
router.put("/update", () => {});
router.delete("/delete", () => {});
router.get("/get", () => {});

export default router;
